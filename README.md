# dot_files

```shell
# download config
git clone https://github.com/abergasov/dot_files.git && cd dot_files && python3 set_config.py

# upload conf to git
python3 rewrite_files.py && git add . && git commit -m '1' && git push origin master
```